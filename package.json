{
  "name": "plancop",
  "version": "0.2.0-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "cd ui && vite build",
    "dev": "cd ui && vite dev",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^22.14.0",
    "@vitest/coverage-v8": "^3.2.4",
    "happy-dom": "^17.0.0",
    "typescript": "~5.8.2",
    "vitest": "^3.2.0"
  }
}
